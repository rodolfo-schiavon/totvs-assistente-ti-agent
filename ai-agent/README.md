# Agente IA — Help Desk

Serviço Python (FastAPI + LangGraph) deployado no Railway com root directory `ai-agent`.

- Health: `/health`
- Chat: `POST /v1/chat` (JWT via frontend BFF)

Variáveis: ver [`docs/AGENT.md`](../docs/AGENT.md).
